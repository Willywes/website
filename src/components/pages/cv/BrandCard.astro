---
interface Props {
  name: string;
  icon: string;
}

const { name, icon } = Astro.props;
---

<div class="flex flex-col items-center gap-1">
  <div class="w-8 h-8">
    <img src={icon} class="w-full h-full object-contain" alt={name} />
  </div>
  <p class="text-xs">{name}</p>
</div>
